<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thesis">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".PopSearchFilter"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".TermAct"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".PopDeactivate"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".PopLI"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".PopOU"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".SettingsActivity" />
        <activity
            android:name=".PopInfoDeact"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".SignupActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LogInActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Owner_Profile_Activity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SearchResultActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--<activity android:name=".HouseProfileActivity" />-->
        <!--<activity-->
            <!--android:name=".AddHouseActivity"-->
            <!--android:theme="@style/AppTheme.NoActionBar" />-->
        <!--<activity-->
            <!--android:name=".AddHouseBooardingHouseInfoActivity"-->
            <!--android:theme="@style/AppTheme.NoActionBar" />-->
        <!--<activity-->
            <!--android:name=".AddHouseDormInfoActivity"-->
            <!--android:theme="@style/AppTheme.NoActionBar" />-->
        <activity
            android:name=".AddHouseApartmentPaymentActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".addHouseApartmentRulesActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AddHouseApartmentPaymentRulesActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AddHouseApartmentPictureAcitivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AddHouseApartmentSaveActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddHouseBoardingHousePictureActivity" />
        <activity android:name=".AddHouseBoardingHouseRuleActivity" />
        <activity android:name=".AddHouseBoardingHousePayment" />
        <activity android:name=".AddHouseBoardingHousePaymentRulesActivity" />
        <activity android:name=".AddHouseBoardingHouseSaveActivity" />
        <activity android:name=".AddHouseDormPictureActivity" />
        <activity android:name=".AddHouseDormRulesActivity" />
        <activity android:name=".AddHouseDormPaymentActivity" />
        <activity android:name=".AddHouseDormPaymentRulesActivity" />
        <activity android:name=".AddHouseDormSaveActivity" />
        <activity android:name=".PostDetailActivity" />
        <!--<activity android:name=".HouseInfoActivity" />-->
        <activity
            android:name=".NavDrawerAct"
            android:parentActivityName=".LogInActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.thesis.LogInActivity" />
        </activity>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyD8wWPdnkB1bmdSXiBUCYSxtLdc9s3KNxA" />

        <activity
            android:name=".AddHousePlacePickerActivity"
            android:label="Map" />

        <!-- <activity -->
        <!-- android:name=".PlacePicker" -->
        <!-- android:label="@string/title_activity_place_picker"></activity> -->
    </application>

</manifest>